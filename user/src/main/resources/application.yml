server:
  port: 8000
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8765/eureka/

spring:
  application:
    name: user
  datasource:
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://www.enricher.space:3306/coode
mybatis:
  mapper-locations: classpath:mapper/*.xml
